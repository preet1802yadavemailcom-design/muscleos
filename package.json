{
  "name": "muscleos-monorepo",
  "private": true,
  "scripts": {
    "build": "npm run db:generate --prefix backend && npm run build --prefix backend",
    "start": "npm run start:prod --prefix backend",
    "postinstall": "npm run db:generate --prefix backend || true"
  }
}
